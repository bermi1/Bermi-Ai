# --- Bermi Desktop (Jarvis) core ---
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
pydantic>=2.7
pydantic-settings>=2.3
python-multipart>=0.0.9

# --- Speech to text (local) ---
faster-whisper>=1.0.3

# --- Text to speech ---
edge-tts>=6.1.12          # natural online voice (default)
pyttsx3>=2.90             # offline fallback

# --- Computer control ---
pyautogui>=0.9.54         # mouse / keyboard / screenshots
psutil>=5.9               # process + system info
pyperclip>=1.8            # clipboard
# On Linux, pyautogui also needs: sudo apt-get install scrot python3-tk python3-dev
